#!/bin/bash
# shellcheck disable=SC1091

if [ ! -d ".venv" ]; then
    python -m venv .venv
    . .venv/bin/activate
    pip install -e .
else
    . .venv/bin/activate
fi

.venv/bin/python -B -m archinstall "$@"
