on: [ push ]
name: Pytest test validation
jobs:
    pytest:
        runs-on: ubuntu-latest
        container:
            image: archlinux/archlinux:latest
            options: --privileged
        steps:
            - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
            - name: Prepare arch
              run: |
                pacman-key --init
                pacman --noconfirm -Sy archlinux-keyring
                pacman --noconfirm -Syyu
                pacman --noconfirm -Sy python-pip python-pyparted pkgconfig gcc
            - run: pip install --break-system-packages --upgrade pip
            - name: Install archinstoo dependencies
              run: pip install --break-system-packages ./archinstoo[dev]
            - name: Test with pytest
              run: pytest -c archinstoo/pyproject.toml archinstoo/tests
